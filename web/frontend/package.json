{
    "name": "rpg-dm-manager",
    "version": "1.0.0",
    "description": "Web management interface for RPG DM Bot",
    "scripts": {
        "dev": "node ./node_modules/typescript/bin/tsc --watch",
        "build": "node ./node_modules/typescript/bin/tsc && npm run copy-html",
        "copy-html": "mkdir -p dist && cp index.html styles.css dist/"
    },
    "devDependencies": {
        "typescript": "^5.9.3"
    }
}
